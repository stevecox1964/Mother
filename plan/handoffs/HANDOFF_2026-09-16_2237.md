# Handoff — Project delete/restore committed and pushed

**Date:** 2026-09-16 22:37
**Branch:** main
**Prior handoff:** [HANDOFF_2026-09-11_1146.md](HANDOFF_2026-09-11_1146.md)

## TL;DR

All work from the reset session is committed and pushed to `origin/main` as `3eb6603`. The working tree is clean. The new UI (project delete/restore, fold, chat scroll) is not yet checked in a browser; do that first.

## What was done this session

- Recapped the uncommitted work left by the session before the reset (it had no handoff of its own).
- Verified: backend tests `126 passed`; `npm run build` in `frontend/` succeeds.
- Told the user to start Mother with `Start-Mother.cmd` (it runs `Start-Mother.ps1` with `-ExecutionPolicy Bypass` and pauses on error).
- Updated `README.md`:
  - Run section: launcher now stops old Mother servers from this folder; errors if another program holds the port.
  - First conversation: fold the open project; trash icon beside **Projects** lists deleted projects; project delete/restore rules; chat scroll-follow behavior.
  - Storage section: `DELETE /api/projects/<id>`, `GET /api/projects/deleted`, `POST /api/projects/<id>/restore`.
- Committed all 14 files as `3eb6603` and pushed to `https://github.com/stevecox1964/Mother.git`.

Work contained in that commit (written in the pre-reset session):
- Soft delete of projects: `projects.deleted_at` column, `Store.delete_project/restore_project/deleted_projects` in `backend/mother/store.py`; `Projects.delete/restore` in `backend/mother/projects.py`; routes in `backend/mother/app.py`.
- `delete_file` model tool with sha256 check and backup in `backend/mother/file_tools.py`.
- Sidebar fold state, delete icon and Deleted projects dialog in `frontend/src/components/Sidebar.jsx` and `ProjectApp.jsx`.
- Chat auto-scroll only when at bottom in `frontend/src/components/App.jsx`.
- `Start-Mother.ps1` kills old Mother python processes before starting.

## In flight

_None._

## Decisions locked in this session

- **Project delete is a soft delete.** Folder, config and conversations stay on disk so restore is lossless.
- **`mother` and `default` projects cannot be deleted.** They are built-in.
- **Delete is refused while a reply runs in that project.** Avoids orphaned runs.
- **Launcher always restarts the server.** So the browser never talks to stale code.
- **Default branch is `main`.** Resolves the prior handoff's branch question.

## Open questions

- **Browser check not done.** Delete, restore and fold have tests only on the backend side.
- **Old demo profiles clutter the Models list** (carried over from prior handoff; user has not answered).
- **Split `ModelSettings.jsx`** (527 lines; carried over; user has not answered).
- **Backlog item 002 (`plan/backlog/002-first-git-commit.md`)** looks done now that commits exist on `main`; confirm and close it.

## Next concrete step

Double-click `Start-Mother.cmd`, create a throwaway project, delete it with the trash icon beside it, then restore it from the trash icon beside **Projects** and confirm its conversations came back.

## Files touched this session

- `README.md` — documented project delete/restore, fold, chat scroll, launcher restart, new API routes.
- `plan/handoffs/HANDOFF_2026-09-16_2237.md` — this file.
- `plan/handoffs/LATEST.md` — points to this file.
- (Committed, not edited this session: the 13 other files listed in `3eb6603`.)

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is: Double-click `Start-Mother.cmd`, create a throwaway project, delete it with the trash icon beside it, then restore it from the trash icon beside **Projects** and confirm its conversations came back.

## Git state at handoff

```
(clean)
```

Recent commits:

```
3eb6603 Add project delete and restore, delete_file tool, launcher restart
94f7492 Document file write and web tools in README
7676007 Add MIT license
faa9415 Add project file write tools and Browserbase web tools
9c7ee59 Initial commit of Mother multi-model harness
```
