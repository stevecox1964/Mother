# Handoff — Read-only Files page added, committed and pushed

**Date:** 2026-09-22 15:05
**Branch:** main
**Prior handoff:** [HANDOFF_2026-09-16_2237.md](HANDOFF_2026-09-16_2237.md)

## TL;DR

Each project now has a **Files** page: a folder tree of the project folder and a read-only file viewer. It is committed as `02ae47c` and pushed to `origin/main`. The user checked it on their real Mother and it works. No work is in flight.

## What was done this session

- Resumed from the 2026-09-16 handoff.
- Backend: two read-only routes in `backend/mother/app.py` (next to `project_document`):
  - `GET /api/files?project_id=<id>` → `workspace.list_files(project_path)`.
  - `GET /api/files/content?project_id=<id>&path=<rel>` → `workspace.snapshot(...)` for one file. Reuses its checks: inside root, `workspace.allowed()`, ≤200 KB, UTF-8, no NUL.
- Frontend: new `frontend/src/components/Files.jsx` (flat paths → nested `<details>` tree; `<pre>` viewer; refresh button). Added `["files", "Files", Files]` to the sidebar pages in `Sidebar.jsx`; added the `files` view and title in `App.jsx`; `.files-*` styles at the end of `frontend/src/style.css` (stacks at ≤700px).
- Test: `test_files_page_lists_and_reads_project_files_only` in `tests/test_projects.py` (lists only allowed files; reads content; rejects `../`, absolute path, `.env`, empty path; unknown project → 404). Full suite: `127 passed`.
- Browser check on a test server (port 5099, temp data dir): tree, folder open, file read all worked. The user then confirmed it on their real server.
- `README.md`: **Files** added to the sidebar list, one paragraph on the Files page, and the two routes in the API paragraph.

## In flight

_None._

## Decisions locked in this session

- **Files page is read-only.** The user asked to view, not edit.
- **It shows the project's `project_path`**, the same folder models see as `/project`. For Mother, that is the repo root.
- **It uses the same filters as model file tools** (`workspace.allowed`, 200 KB limit). The page shows exactly what models can read. It does not follow model access settings.
- **Plain text `<pre>` view, no Markdown rendering or syntax highlighting.** Keep it simple.

## Open questions

- **`.pytest_cache` shows in the Mother project's Files tree** (and models can read it). Option: add `.pytest_cache` to `SKIP` in `backend/mother/workspace.py`. The user has not decided.
- **The browser check of project delete/restore is still not done** (from the prior handoff).
- **Old demo profiles clutter the Models list** (carried over; not answered).
- **Split `ModelSettings.jsx`** (527 lines; carried over; not answered).
- **Backlog item `plan/backlog/002-first-git-commit.md`** looks done; confirm and close it.

## Next concrete step

Ask the user which open question to take next. If they have no preference, do the browser check from the prior handoff: start `Start-Mother.cmd`, create a throwaway project, delete it with its trash icon, restore it from the trash icon beside **Projects**, and confirm its conversations came back.

## Files touched this session

- `backend/mother/app.py` — added `/api/files` and `/api/files/content` routes.
- `frontend/src/components/Files.jsx` — new Files page (tree and read-only viewer).
- `frontend/src/components/Sidebar.jsx` — Files item in the project tree.
- `frontend/src/components/App.jsx` — `files` view and header title.
- `frontend/src/style.css` — `.files-*` styles.
- `tests/test_projects.py` — new Files route test.
- `README.md` — documented the Files page and routes.
- `plan/handoffs/HANDOFF_2026-09-22_1505.md` — this file.
- `plan/handoffs/LATEST.md` — points to this file.

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is: Ask the user which open question to take next; if no preference, do the browser check of project delete/restore.

## Git state at handoff

```
 M plan/handoffs/LATEST.md
?? plan/handoffs/HANDOFF_2026-09-16_2237.md
?? plan/handoffs/HANDOFF_2026-09-22_1505.md
```

Recent commits:

```
02ae47c Add read-only Files page for project folders
3eb6603 Add project delete and restore, delete_file tool, launcher restart
94f7492 Document file write and web tools in README
7676007 Add MIT license
faa9415 Add project file write tools and Browserbase web tools
```
